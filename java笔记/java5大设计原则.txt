依赖倒置：
依赖关系被倒置：传统的结构化程序设计中，高层模块总是依赖于低层模块。

依赖关系倒置：高层模板与底层模板都依赖抽象接口层
里氏替换原则：
对一个类的扩展构造一个新的子类，不能对现有的系统引入新的错误。
接口分离原则:
1.不要把不使用的方法塞进同一个接口，遵循最小接口原则
2.当一个接口继承另一个接口，则继承后的接口也不能包含用户不使用的方法。
单一职责：
只让一个类负责一个职责（如果一个类需要改变，改变它的理由永远只有一个。如果存在多个改变它的理由，就需要重新设计该类）
开闭原则：
开放扩展，关闭修改。